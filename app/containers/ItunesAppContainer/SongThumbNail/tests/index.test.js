/**
 *
 * Tests for SongThumbNail
 *
 */

// import React from 'react';
// import { fireEvent } from '@testing-library/dom'
// import { renderWithIntl } from '@utils/testUtils';
// import SongThumbNail from '../index';

describe('<SongThumbNail />', () => {
  it('should render and match the snapshot', () => {
    // const { baseElement } = renderWithIntl(<SongThumbNail />);
    // expect(baseElement).toMatchSnapshot();
  });

  it('should contain 1 SongThumbNail component', () => {
    // const { getAllByTestId } = renderWithIntl(<SongThumbNail />);
    // expect(getAllByTestId('song-thumb-nail').length).toBe(1);
  });
});
